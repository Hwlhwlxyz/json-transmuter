{
  "name": "json-transmuter",
  "version": "0.0.1",
  "description": "",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "mocha -r ts-node/register test/**/*.test.ts",
    "testparallel": "mocha -r ts-node/register test/**/*.test.ts --parallel",
    "mocha": "mocha -r ts-node/register ",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test"
  },
  "keywords": ["json", "transmuter", "transform"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "files": [
    "lib/**/*"
  ]
}
